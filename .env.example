# No client-side API keys needed!
# The Finnhub key is set as a Cloudflare Pages secret:
#   npx wrangler pages secret put FINNHUB_KEY --project-name fintrack
# It lives only server-side in the /api/quotes proxy function.
